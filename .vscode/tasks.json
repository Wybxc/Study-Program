{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make",
            "type": "shell",
            "command": "cd /d \"${workspaceFolder}\" && make",
            "group": {
                "kind": "test",
                "isDefault": true
            }
        },
        {
            "label": "make clean",
            "type": "shell",
            "command": "cd /d \"${workspaceFolder}\" && make clean",
            "group": "test"
        }
    ]
}